apiVersion: v1
kind: ConfigMap
metadata:
  name: identity-config
  namespace: identity-reconciliation

data:
  PORT: "3000"
  DB_HOST: "host.docker.internal"
  DB_PORT: "5432"
  DB_NAME: "identity_db"
  DB_USER: "postgres"